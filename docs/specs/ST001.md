# ST001 检查黑名单

## 步骤描述

1.获取黑名单检查标志与服务状态：根据{接口服务代码}、{接口服务类型}查询【核心服务定义表】获取[服务信息列表]，包括$黑名单检查标志$与$服务状态$
2.检查接口是否允许检查黑名单：若[服务信息列表]中存在$服务状态$等于"A-生效"，且$黑名单检查标志$等于“Y-是”的记录，则跳转至子步骤《获取黑名单信息》，否则返回检查结果为“通过”。

3.获取黑名单信息：根据{客户号}或{证件类型}、{证件号码}或{账号}查询【名单信息表】获取$黑名单状态$为“生效”的$名单类型代码$，将获取的信息赋值给[黑名单信息]。

4.检查是否存在黑名单：若[黑名单信息]不为空，则跳转至子步骤《获取黑名单检查规则编号》，否则返回检查结果为“通过”。
5.获取黑名单检查规则编号：根据$名单类型代码$查询【名单类型表】获取对应的$黑名单检查规则编号$。

6.检查黑名单检查规则编号：若[黑名单检查规则编号]不等于空，则跳转至子步骤《获取黑名单限制操作标识》，否则返回检查结果为“通过”。
7.获取黑名单限制操作标识：根据[黑名单检查规则编号]查询【名单限制规则表】获取[名单限制规则信息]，其中必须包括$黑名单限制操作标识$$介质$$限制机构范围$
8.检查黑名单是否是检查类：若[名单限制规则信息]的$黑名单限制操作标识$等于"E-检查类"，则跳转至子步骤《获取事件类型》，否则返回检查结果为“通过”。

9.获取事件类型：根据{事件类型}、{交易类型}、{渠道类型}、{交易码}、{服务代码}、{接口服务类型}、{接口服务代码}查询【名单检查范围表】获取所有的$事件类型$

10.获取名单不检查范围列表：根据{交易类型}、{渠道类型}、{交易码}、{服务代码}、{接口服务类型}、{接口服务代码}以及上一步提取的[事件类型]查询【名单不检查范围表】获取[名单不检查范围信息]

11.检查名单不检查范围：若[名单不检查范围信息]不为空，则返回检查结果为“通过”；否则，执行子步骤《检查介质》和子步骤《检查机构》

12.检查介质：若上送的{凭证种类}在[名单限制规则信息]的$介质$范围内，则执行《获取黑名单处理方式》，否则返回检查结果为“通过”。

13.检查机构：若[名单限制规则信息]的$限制机构范围$等于“B-下级机构”，且{账号}的$账户开立行行号$等于当前{交易机构}或者当前{交易机构}的下级机构，则执行《获取黑名单处理方式》，否则返回检查结果为“通过”。

14.获取黑名单处理方式：根据[黑名单检查规则编号]查询【黑名单信息】获取$处理方式$

15.检查处理方式：a.若[处理方式]等于"拒绝"，则返回检查结果为“拒绝”；b.若[处理方式]等于"授权"，则返回检查结果为“授权”；c.若[处理方式]等于"提醒"，则返回检查结果为“提醒”。

## 输入

| 字段名 | 类型 | 标记 | 说明 |
|--------|------|------|------|
| docClass | com.dcits.blacklist.enums.DocClass | 非必填 | 凭证种类 |
| tranBranch | java.lang.String | 必填 | 交易机构号 |
| documentType | com.dcits.blacklist.enums.DocumentType | 必填 | 证件类型 |
| documentId | java.lang.String | 必填 | 证件号码 |
| baseAcctNo | java.lang.String | 非必填 | 账号 |
| acctBranch | com.dcits.blacklist.enums.AcctBranch | 必填 | 账户开立行行号 |

| sourceType | com.dcits.blacklist.enums.SourceType | 必填 | 渠道类型 |
| programId | java.lang.String | 必填 | 交易代码 |
| tranType | com.dcits.blacklist.enums.TranType | 必填 | 交易类型 |
| eventType | java.lang.String | 必填 | 事件类型 |
| cardMedium | java.lang.String | 必填 | 卡介质 |
| resBranchRange | com.dcits.blacklist.enums.ResBranchRange | 必填 | 限制机构范围 |

| serviceCode | java.lang.String | 必填 | 服务代码 |
| messageType | java.lang.String | 必填 | 接口服务类型 |
| messageCode | java.lang.String | 必填 | 接口服务代码 |
| blacklistCheckFlag | java.lang.String | 必填 | 黑名单检查标志 |
| serviceStatus | java.lang.String | 必填 | 服务状态 |

| clientNo | java.lang.String | 非必填 | 客户号 |

## 输出

| 字段名 | 类型 | 标记 | 说明 |
|--------|------|------|------|
| dealFlow | com.dcits.blacklist.enums.DealFlow | 非必填 | 处理方式 |

## 相关枚举

| 枚举 | 别名 |
|------|------|
| 凭证种类 | 凭证种类、输入数据-凭证种类 |
| 证件类型 | 证件类型、输入数据-证件类型 |
| 内部机构编号 | 账户开立行行号、输入数据-账户开立行行号 |
| 渠道类型 | 渠道类型、输入数据-渠道类型 |
| 交易类型 | 交易类型、输入数据-交易类型 |
| 限制机构范围 | 限制机构范围、输入数据-限制机构范围 |
| 处理方式 | 处理方式、输出数据-处理方式 |

## 提示

- {凭证种类,输入数据-凭证种类}取值来源于代码[凭证种类]，对应枚举类-类名：com.dcits.blacklist.enums.DocClass
- {证件类型,输入数据-证件类型}取值来源于代码[证件类型]，对应枚举类-类名：com.dcits.blacklist.enums.DocumentType
- {账户开立行行号,输入数据-账户开立行行号}取值来源于代码[内部机构编号]，对应枚举类-类名：com.dcits.blacklist.enums.AcctBranch
- {渠道类型,输入数据-渠道类型}取值来源于代码[渠道类型]，对应枚举类-类名：com.dcits.blacklist.enums.SourceType
- {交易类型,输入数据-交易类型}取值来源于代码[交易类型]，对应枚举类-类名：com.dcits.blacklist.enums.TranType
- {限制机构范围,输入数据-限制机构范围}取值来源于代码[限制机构范围]，对应枚举类-类名：com.dcits.blacklist.enums.ResBranchRange
- {处理方式,输出数据-处理方式}取值来源于代码[处理方式]，对应枚举类-类名：com.dcits.blacklist.enums.DealFlow
